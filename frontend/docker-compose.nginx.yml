# Frontend with Nginx Docker Compose
services:
  app:
    build:
      context: ..
      dockerfile: frontend/Dockerfile
      target: prod-nginx
    ports:
      - "80:80"
    environment:
      NODE_ENV: production
    env_file:
      - .env.prod
    restart: unless-stopped
